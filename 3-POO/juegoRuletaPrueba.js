let numerosDeRuleta = new Array(37);

for (let i = 0; i <= 37; i++) {
    numerosDeRuleta[i] = i;
    }

for (let indice=0; indice<37; indice++) {
        console.log("El numero ingresado en la posición", indice, "es:", numerosDeRuleta[indice]);
    }
