let readlineSync =require('readline-sync');
let texto = readlineSync.question("Ingrese un texto: ");
let cantidad = texto.length;
let indice = 0;
 if (texto.substring(1) == "s") {
     console.log("mierda")
 }

